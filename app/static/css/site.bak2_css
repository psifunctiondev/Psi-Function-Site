/* =========================
   Base / Layout
========================= */

body {
  margin: 0;
  font-family: var(--font-sans);
  background: var(--color-bg);
  color: var(--color-text);
  line-height: 1.6;
}

main {
  padding: var(--space-8) 0;
}

.container {
  max-width: var(--container-max);
  margin: 0 auto;
  padding-left: var(--space-4);
  padding-right: var(--space-4);
}

.section-stack {
  margin-top: var(--space-8);
}

/* =========================
   Header
========================= */

.site-header {
  border-bottom: 1px solid var(--color-border);
  background: var(--color-surface);
  position: sticky;
  top: 0;
  z-index: var(--z-sticky, 100);
  backdrop-filter: blur(10px);
}

.site-header__inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-4);
  min-height: 72px;
  padding-top: var(--space-3);
  padding-bottom: var(--space-3);
  flex-wrap: wrap;
}

.site-logo {
  display: inline-flex;
  flex-direction: column;
  gap: 0.15rem;
  text-decoration: none;
}

.site-logo__wordmark {
  font-family: 'Michroma', sans-serif;
  font-size: 0.95rem;
  line-height: 1.1;
  color: var(--color-heading);
}

.site-logo__tagline {
  font-size: var(--text-sm);
  color: var(--color-text-muted);
}

.site-nav {
  display: flex;
  align-items: center;
  gap: var(--space-4);
  flex-wrap: wrap;
}

.site-nav__link {
  color: var(--color-text);
  text-decoration: none;
  font-size: 0.95rem;
  line-height: 1;
}

.site-nav__link:hover {
  color: var(--color-link-hover);
}

.site-header__controls {
  display: flex;
  align-items: center;
  gap: var(--space-3);
}

@media (max-width: 767px) {
  .site-header__inner {
    align-items: flex-start;
  }

  .site-nav {
    width: 100%;
    order: 3;
    padding-top: var(--space-2);
  }

  .site-header__controls {
    margin-left: auto;
  }
}

.site-header .container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-top: var(--space-4);
  padding-bottom: var(--space-4);
}

.site-logo {
  font-family: 'Michroma', sans-serif;
  font-size: 0.9rem;
  text-decoration: none;
  color: var(--color-heading);
}

/* =========================
   Hero
========================= */

.home-hero__grid {
  display: grid;
  gap: var(--space-6);
}

@media (min-width: 768px) {
  .home-hero__grid {
    grid-template-columns: 1.2fr 1fr;
    align-items: center;
  }
}

.home-hero__title {
  font-size: 2rem;
  line-height: 1.2;
  color: var(--color-heading);
  margin-bottom: var(--space-4);
}

.home-hero__lede {
  color: var(--color-text-muted);
  margin-bottom: var(--space-6);
}

.home-hero__actions {
  display: flex;
  gap: var(--space-3);
  flex-wrap: wrap;
}

.home-hero__media {
  padding: var(--space-4);
  border-radius: var(--radius-md);
}

/* =========================
   Typography helpers
========================= */

.eyebrow,
.section-kicker {
  font-size: var(--text-sm);
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--color-accent);
  margin-bottom: var(--space-2);
}

.section-title {
  font-size: 1.5rem;
  color: var(--color-heading);
  margin-bottom: var(--space-3);
}

.section-text {
  color: var(--color-text-muted);
}

.prose-stack > * + * {
  margin-top: var(--space-3);
}

/* =========================
   Surfaces
========================= */

.surface {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: var(--space-6);
}

.surface--hero {
  background: var(--color-surface-elevated);
}

.surface--section {
  background: var(--color-surface);
}

.surface--accent {
  background: var(--color-surface-elevated);
  border-color: var(--color-border-strong);
}

/* =========================
   Cards
========================= */

.card-grid {
  display: grid;
  gap: var(--space-4);
}

@media (min-width: 768px) {
  .card-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

.card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: var(--space-5);
  transition: transform 0.15s ease, box-shadow 0.15s ease;
}

.card:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
  background: var(--color-surface-elevated);
}

.card__title {
  color: var(--color-heading);
  margin-bottom: var(--space-2);
}

.card__text {
  color: var(--color-text-muted);
}

/* =========================
   Buttons
========================= */

.button {
  display: inline-block;
  padding: 0.5rem 0.9rem;
  border-radius: var(--radius-md);
  font-size: 0.9rem;
  text-decoration: none;
  border: 1px solid transparent;
  cursor: pointer;
}

.button--primary {
  background: var(--color-primary);
  color: var(--color-on-primary);
}

.button--primary:hover {
  background: var(--color-primary-hover);
}

.button--secondary {
  background: transparent;
  border-color: var(--color-border);
  color: var(--color-text);
}

.button--secondary:hover {
  background: var(--color-surface-elevated);
}

/* =========================
   CTA band
========================= */

.cta-band {
  background: var(--gradient-signal);
  border-radius: var(--radius-md);
  padding: var(--space-6);
  display: flex;
  flex-direction: column;
  gap: var(--space-4);
}

@media (min-width: 768px) {
  .cta-band {
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
  }
}

.cta-band__content {
  max-width: 600px;
}

.cta-band__actions {
  display: flex;
  gap: var(--space-3);
  flex-wrap: wrap;
}

/* =========================
   Theme Switcher (from earlier)
========================= */

.theme-switcher {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
}

.theme-switcher__label {
  color: var(--color-text-muted);
  font-size: 0.75rem;
}

.theme-switcher__select {
  padding: 0.35rem 0.6rem;
  border-radius: var(--radius-sm);
  border: 1px solid var(--color-border);
  background: var(--color-surface);
  color: var(--color-text);
}

.theme-switcher__select:focus {
  outline: 2px solid var(--color-focus-ring);
}

.theme-live-preview {
  padding: var(--space-6);
  border-radius: var(--radius-xl);
  background: var(--color-bg);
  color: var(--color-text);
  border: var(--border-width-thin) solid var(--color-border);
}

/* Hero / primary surface */
.theme-live-preview__hero {
  padding: var(--space-5);
  border-radius: var(--radius-lg);
  background: var(--color-surface);
  border: var(--border-width-thin) solid var(--color-border);
}

/* Label */
.theme-live-preview__eyebrow {
  margin-bottom: var(--space-2);
  font-size: var(--text-xs);
  font-weight: var(--font-weight-bold);
  letter-spacing: var(--letter-spacing-wide);
  text-transform: uppercase;
  color: var(--color-text-muted);
}

/* Headline */
.theme-live-preview__title {
  margin-bottom: var(--space-3);
  font-family: var(--font-family-display);
  font-size: var(--text-xl);
  line-height: var(--line-height-snug);
  color: var(--color-heading);
}

/* Body */
.theme-live-preview__body {
  font-family: var(--font-family-sans);
  line-height: var(--line-height-relaxed);
  color: var(--color-text);
}

/* Actions */
.theme-live-preview__actions {
  display: flex;
  gap: var(--space-3);
  align-items: center;
  margin-top: var(--space-4);
}

/* Primary button-like element */
.theme-live-preview__badge {
  padding: 0.4rem 0.75rem;
  border-radius: var(--radius-pill);
  background: var(--color-primary);
  color: var(--color-on-primary);
  font-weight: var(--font-weight-semibold);
}

/* Link */
.theme-live-preview__link {
  color: var(--color-link);
  font-weight: var(--font-weight-semibold);
  text-decoration: none;
}

.theme-live-preview__link:hover {
  color: var(--color-link-hover);
  text-decoration: underline;
}

/* Grid */
.theme-live-preview__grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: var(--space-4);
  margin-top: var(--space-4);
}

/* Cards */
.theme-live-preview__card {
  padding: var(--space-4);
  border-radius: var(--radius-lg);
  background: var(--color-surface-elevated);
  border: var(--border-width-thin) solid var(--color-border);
}

.theme-live-preview__card-label {
  margin-bottom: var(--space-2);
  font-size: var(--text-xs);
  font-weight: var(--font-weight-bold);
  letter-spacing: var(--letter-spacing-wide);
  text-transform: uppercase;
  color: var(--color-text-muted);
}

.theme-live-preview__headline {
  font-family: var(--font-family-display);
  font-size: var(--text-md);
  color: var(--color-heading);
}

.theme-live-preview__copy {
  line-height: var(--line-height-relaxed);
  color: var(--color-text);
}

.theme-live-preview__muted {
  line-height: var(--line-height-relaxed);
  color: var(--color-text-muted);
}
