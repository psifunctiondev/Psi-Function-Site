"""
DrifterBot test package.

Imports route through `agents.driftbot.*` and rely on the
`tests/driftbot/conftest.py` sys.path insertion to find the
package root. This file just makes the directory importable
when pytest is run with `--import-mode=importlib`.
"""
